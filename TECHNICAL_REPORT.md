# ПЛАН И СТАТУС

Этот файл был восстановлен после аварийной потери исходников.

- Фаза 1: восстановлена базовая DB-слой архитектура (`db/`, репозитории, миграционный скрипт).
- Фаза 2: восстановлена тестовая инфраструктура (`pytest.ini`, `tests/`, smoke/unit/integration).
- Фаза 6: восстановлен FastAPI каркас (`api/main.py`, `api/routers/*`, `run_api.py`).

Подробный план работ ведется в текущем диалоге и в новых документах в `docs/`.
